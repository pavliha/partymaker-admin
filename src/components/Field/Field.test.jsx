describe('<Field />', () => {

  it('should pass props to field component', () => {
    // const componentFn = jest.fn()
    // mount(
    //   <Formik onSubmit={() => {}} initialValues={{ test: 'field' }}>
    //     <Form>
    //       <Field name="test" component={componentFn} />
    //     </Form>
    //   </Formik>
    // )
    // expect(componentFn).toBeCalledWith({ name: 'test' })
  })

})
